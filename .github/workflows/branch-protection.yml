name: Branch protection
# Issue #521: main の保護設定が弱まっていないかを見張る。
#
# なぜ要るか: branch protection は GitHub の**設定**であって、このリポジトリの中身ではない。
# 弱めるのは API 1 回で済み、diff にもレビューにも CI にも一切現れない。実際 `enforce_admins` は false のまま
# 放置され、必須チェックを1つも通していない commit が 3 つ main に入った（#521）。
# push は成功し、git は `remote: Bypassed rule violations for refs/heads/main` としか言わない。
#
# この workflow は素通りを不可能にはしない（それは enforce_admins の仕事で、管理者はいつでも戻せる）。
# **隠れて通れなくする**: 弱まっていれば次の実行で Issue が開く。
# 作業合意「防御は不可能にすることではなく、隠れて通れなくすること」。
#
# 検査そのものは deploy/monitor/branch-protection.sh（テスト: deploy/test/branch-protection.test.sh）。
# Issue の開閉は monitor.yml と同じ deploy/monitor/report.sh に任せる（タイトルが同一性なので溜まらない）。
# 直し方と緊急時の逃げ道は docs/ops/deploy.md「main の保護設定」。
on:
  schedule:
    - cron: "23 21 * * *"   # 毎日 06:23 JST。monitor.yml の 10 分刻みとぶつからない分を選ぶ
  pull_request:
    # 検査そのものを触る PR では、その場で走らせて壊れていないことを見る
    paths:
      - ".github/workflows/branch-protection.yml"
      - "deploy/monitor/branch-protection.sh"
      - "deploy/monitor/branch-protection-report.sh"
      - "deploy/test/branch-protection.test.sh"
      - "deploy/test/branch-protection-report.test.sh"
  workflow_dispatch:
# GITHUB_TOKEN に branch protection の読み取り権限は与えられない。
# `administration: read` を書いて実測したところ、GitHub はワークフローを**構文として拒否**した:
#   HTTP 422: failed to parse workflow: (Line: 31, Col: 3): Unexpected value 'administration'
# `administration` は GITHUB_TOKEN の permissions に存在しないキーで、書くと**この workflow 自体が
# 一切動かなくなる**（#540）。読み取りには fine-grained PAT が要る——**人間の作業**で、
# 手順は docs/ops/deploy.md「main の保護設定」。PAT が置かれるまでは exit 2（読めない）として
# 報告し続ける。**何も見ていない状態にはしない。**
permissions:
  contents: read
  issues: write
concurrency:
  group: branch-protection
  cancel-in-progress: false
jobs:
  guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: unit tests (gh / report.sh are stubs, no network)
        run: |
          bash deploy/test/branch-protection.test.sh
          bash deploy/test/branch-protection-report.test.sh
      # PR では実際の設定を読まない: fork からの PR に issues:write は渡らず、Issue の開閉もしたくない。
      # 設定そのものの確認は schedule と workflow_dispatch でやる。
      # 判定と報告の対応（どの終了コードでどの Issue を開閉するか）は
      # deploy/monitor/branch-protection-report.sh にある。**ここに直接書かない**:
      # インラインの `run:` に書いていた間、それを検査するものが1つも無く、レビューが
      # タイトルの入れ替え・rc=2 分岐の削除・`set +e` の削除の3つを入れても
      # テストは 19 passed のまま素通りした（#546 レビュー）。
      # いまは deploy/test/branch-protection-report.test.sh が rc=0/1/2/想定外の4通りを固定している。
      - name: check main's protection → Issue
        if: github.event_name != 'pull_request'
        env:
          # branch protection の読み取りには管理権限が要る。**`GITHUB_TOKEN` では読めない**
          # （上の permissions の注記を参照）。人間が fine-grained PAT を置けば自動的にそちらを使う。
          GH_TOKEN: ${{ secrets.BRANCH_PROTECTION_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          bash deploy/monitor/branch-protection-report.sh \
            "$GITHUB_REPOSITORY" main \
            "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
