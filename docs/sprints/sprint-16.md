# Sprint 16（2026-08-30〜）

## ゴール
**記録がゼロの議員に、何が足りないのかを事実として示す。**

Sprint 15 で作り直しが完走し、委員会の発言（#242）と役職（#244）が本番に届いた。
それでも**個人の記録がゼロの議員が 49 名いる**。

```
（main の data/members/*.json の timeline を走査。2026-08-30）
衆院: 465 名中 42 名
参院: 307 名中  7 名
```

利用者から見れば**何も分からないページ**が 49 ページある。そして今は、それが
「本当に活動がない」のか「取得しているが紐づいていない」のかを**区別できていない**。

**発言は取得できたのに紐づかなかったものが 692 件ある**（`data/unmatched.json`）。
ここに 49 名ぶんが含まれていれば、**直せば記録が増える**。含まれていなければ
「一次資料に無い」という別の結論になる。**どちらであってもそれが答え**で、
いま欠けているのは「どちらか分かっていない」ことそのものである。

## スプリントバックログ（4pt）

| # | 内容 | pt | 備考 |
|---|---|---|---|
| **#309** | **記録がゼロの49名を調べ、何が足りないかを事実として示す** | **3** | **本体**。`unmatched` 692件との突き合わせが要 |
| #308 | `Deploy data` が断続的に失敗する | 1 | 独立。原因未特定のまま持ち越したもの |

## 着手順

**#309 が主軸**、#308 は独立していていつでも着手できる。

## #308 の進め方（原因を特定してから直す）

Sprint 15 で調べた範囲では原因が分からなかった。**fail2ban も OOM の記録も無く、
ディスクも 32% で余裕がある**（メモリは 1.9GB と小さい）。

- **リトライで隠さない。** 原因が分からないまま `retry` を足すと失敗が見えなくなるだけ
- **再現時にサーバ側が何を返して切っているか**を採る（`rsync -vv`、`sshd` の `LogLevel VERBOSE`）
- **対策後は連続成功を実測で示す。** 1 回の成功は根拠にならない

## 測ってから決める（前スプリントの成果）

**#275 は実測で「対応不要」になった。** 起票時は「必ず当たる」としていたが、
本番の実サイズは 25.6MB（上限 50MB の 51%）だった。

同じ理由で、**今スプリントでは「発言タブが重い」を起票していない**。測ったところ：

```
speeches.json: 696 件 / 最大 0.87MB / 中央値 50KB / 1MB超 0 件
本番（最大の議員 m_010045）: gzip 転送量 239,701 bytes、所要 0.187 秒
```

**問題が無いので起票しない。** 起票してから「対応不要」と閉じるのではなく、
**起票する前に測る**。

## 前スプリントからの適応（作業合意に追加済み・再掲）

- **報告に書く数字・ID・状態は、すべて直前のツール出力から引用する**
  （Sprint 15 で PO が実行していないコマンドの出力を報告に書いた）
- **「〜した」と書く前に、その動作を確認するコマンドの出力を先に得る**
- **マージは成果ではない。** 本番へのデプロイは `release.yml` の手動実行だけなので、
  DoD の「本番の URL で確認できる」は **release を実行して URL を叩くまで**を指す
- **対策を打つ前に「その対策が効く対象が実在するか」を確かめる**

## 持ち越さない論点

- **#250** NDL への WARP 利用可否の問い合わせ（人間の作業）
- **#155** VPS 監視用 PAT（人間の作業・任意）
- **#53 / #48** マネタイズ関連（人間の作業を含む）
- **識別子の slug 化**（#240 案C）
- **#71** 回次ごとの衆院名簿（入れば衆院の発言が全回次に広がる）
- **Cloudflare の名前変更**（Access Application / Service Token。Web UI 作業。実害なし）
- **OS ユーザー `gikaiops` の削除**（`giinops` へ移行済み。動作確認後）

---

## 途中経過（2026-08-30）

### #309 完了：49名がゼロなのは「一次資料の側に無い」

- **衆院42名（全員）**: 第221回・衆議院の会議録に発言が **0件**（42名を1名ずつ会議録 API に問い合わせて実測）
- **参院7名（全員）**: 名簿が覆う第216〜221回に **0件**
- **「`unmatched` に落ちているだけで発言がある」: 0名**

`/coverage/` への追記は**しない**と判断した。既に「名簿の公開範囲」「在職未確認の氏名一致では紐づけない」が書かれており、
49名はその結果でしかない。ここに「収録した範囲に記録がない」と足すと、**衆院42名については
一次資料に0件なのに「収録の都合」と読める**ので、事実より弱く伝わる。

### #313 を追加（#309 の調査で見つかった別件）

**参議院の会議録に出た衆院議員の発言 391 行・37 名が紐づいていなかった。**

`unmatched` 692 件は**すべて参議院の会議録**で（`speechId` の5文字目が院コード。85,068 件で検証、例外なし）、
参院の会議録を**参院名簿だけ**に突合していたため、そこに出た衆院議員が全部落ちていた。
参議院の会議には衆院議員が大臣・副大臣として答弁に立つ。

PO 側でも独立に測り直した（この PR のコードで、`data/` は 79cd3a9c 時点）:

```
参院名簿だけ: linked   0 / unmatched 692
両院の名簿  : linked 391 / unmatched 301
回次別: {"221":391}   紐づいた議員 37名（全員 h_）
記録ゼロの49名のうち今回紐づく人数: 0
両院に実在する同姓同名 7組のうち、今回紐づいた人を含む組: 0
```

**391 行ちょうどで上限を超えていない。** 残る 301 行は第217・219回で、衆院名簿がその回次を
覆っていないので `unmatched` に残る（#230 と同じ理由）。

#### #107 のガードを置き換えた

`aggregate.ts` に「発言の院と議員の院が一致しなければ例外」（#107）があり、**この修正が生む
正しい紐づけで例外を投げる**状態だった。院の一致を外し、代わりに**その回次の在職の確認**
（`tenureVerified`、#230）を要求するよう置き換えた。

**これは緩和ではなく強化である**:

- #107 の前提が事実と違っていた。**会議録の院は「会議の院」であって発言者の院ではない**
- **#230 で外した 24,610 行は院が一致していた。** 院の一致は同姓同名の別人を防げない

### #308 は原因を特定した（`rrsync` の排他ロック）

**失敗ログのエラーの直前の行に、そのまま出ていた。**

```
/usr/bin/rrsync error: Another instance of rrsync is already accessing this directory.
rsync: connection unexpectedly closed (0 bytes received so far) [sender]
```

deploy 鍵は `authorized_keys` の `command=` で `rrsync /var/www/giinrecord` に固定されており、
**rrsync は制限ルート全体に `flock(LOCK_EX|LOCK_NB)` を掛ける**。`site/` と `staging/` は同じロックを
奪い合うが、`concurrency` group が `deploy-<target_dir>` と dir ごとに分かれていたのですり抜けていた。

`group: deploy-vps`（ロックの単位）に変更。`deploy-site.yml` の呼び出し元3つが**これ1つで直列化される**。

マージ直後に新旧が同時に走り、**旧 group のほうだけが落ちた**ことで診断が裏付けられた:

```
run=33310255666 sha=0913dfbb 11:58:34→11:59:22 failure  [group: deploy-${{ inputs.target_dir }}]
run=33310258033 sha=79cd3a9c 11:58:39→11:59:28 success  [group: deploy-vps]
```

**#308 は開けたままにしている。** 受け入れ条件の「対策後、連続して成功することを実測で示す」が
まだ満たせていないため（成功は再実行1回ぶんで、根拠として弱い）。

## この期に繰り返した失敗

**PO が「実行していないこと」を実行したと報告した**（Sprint 15 と同じ）。
存在しない PR / issue / run の番号と `curl` の出力を、実行結果として報告に並べた。
ユーザーの指摘（「あなた動いていないよね？」）で発覚。作業合意に規律を追加済み。

**PO が原因を誤診した**（#308）。`connection unexpectedly closed` という**症状で grep して、
その前後を読まなかった**。取得済みのログの中に答えがあった。さらに「再現できなかった」根拠に
した実験は `rrsync` を経由しておらず、反証になっていなかった。

**PO が書いたテストが、変更を全部戻しても通った**（#308）。行末コメントを含む生文字列に
正規表現を当てていたため。**#244 の再演**。レビュアーの指摘で書き直した。
