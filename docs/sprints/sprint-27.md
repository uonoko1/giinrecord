# Sprint 27（2026-09-09〜09-13）

<!--
**この文書は進行中に書き始めている。**
Sprint 26 の「落としたもの」に「ボードを一度も動かしていなかった」と書いた回の次なので、
**終わってからまとめて書くのではなく、動かしながら書く**ことにした。
終わったら「進行中」を外し、数字を確定させる。
-->

## ゴール

**滋賀を実装できる状態まで持っていく**——ただし「読める」と言い切る前に、
**#689 が 2 本で出した結論を 147 本で確かめ、さらに「数が合う」と「対応が正しい」を分けて確かめる。**

**あわせて、既存 7 県の PDF 読みに残っていた穴（CTM・表題の照合・行送り）を塞ぐ。**

## 本番に出したもの

**この回はまだ本番に何も出していない。**

**理由**: **この回のマージ 43 件はすべて ETL の内部・検査・調査文書で、`data/` も `apps/web` も変えていない。**
`released` タグは `044f42af` のままで、**`origin/main` から 51 コミット遅れている**（2026-09-13 実測）。

**`data/` を変えていないことは実測で確かめてある**——#717 の担当者（と PO）が本番の PDF を取りに行って
高知・三重を再生成し、**`fetchedAt` 以外の変更行が 0 行**であることを両県で示した。

**Sprint 25 で出したものはそのまま本番で動いている**（2026-09-09 実測、UA を名乗って 1 秒間隔）:

| URL | 実測 |
|---|---|
| `/` `/about` `/coverage` `/members` `/assemblies` `/rollcalls` `/terms` `/privacy` | **200**（8/8） |
| `/assemblies/pref-32`（島根）`/assemblies/pref-39`（高知） | **200** |
| 実在しない URL（`/no-such-page-po-check`） | **404** |

**#654 の中間状態は本番に残ったままである**——`/__not-found` が **200 で直接開ける**（実測）。
`site.conf` の反映が人間の ssh 待ちなので、この回でも解消していない。

## マージしたもの（43 PR）

**PDF 読みの穴（この回の主題）**

- **#693 / PR #698** `pdf-table` が `constructPath` の CTM を無視していた。`q`/`Q`/`cm` を辿って持ち回るように
- **#700 / PR #701** 三重・高知の `glyphs.ts` でも罫線に CTM を掛ける
- **#695 / PR #696** PDF の表題と会期名を照合する（徳島の年・島根・宮城の年月・三重の呼び出し口）
- **#703 / PR #706** 高知の `leading` を固定し、**初期値が読まれない理由を実測で残す**

**滋賀の調査**

- **#689 / PR #692** 滋賀・佐賀は「読めない」ではなかった——罫線で割れる／年では切れない
- **#694 / PR #704** **147 本で「記号数＝罫線セル数」を測り直した**（459 行一致・不一致 0。ただし型は 4 つある）

**他県の調査**

- **#688 / PR #690** 総数のみ 3 県の会議録本文を確かめた。**北海道・長野は起立者名なしを実読、愛知は `robots.txt` で到達不可**

**Sprint 26 の積み残し**（この回の頭でマージ）

- **#670 / #671 / #674 / #679 / #680 / #682**（調査・検査）と **#665 / #667 / #669 / #675 / #676 / #677 / #681 / #685 / #691**（索引と記録）

**その他**

- **#702** `sharp` 0.35.3 → 0.35.4（GHSA-rgj7-g3m4-5g8c、libheif の high 級）
- **#697** 「1 段広げる」の 6 例目——生データを読む前に、既にあるパーサの出力を見る
- **#699** **実装とテストが同じ「存在しない名前」を参照すると両側 `undefined` で通る**

### 09-09 以降にマージしたもの（19 PR）

**PDF 読みの穴（続き）**

- **#707 / PR #712** 生の `'` / `"` と 0 でない `Tw` を例外にする。**本当の穴は `Tw` だった**（誰も見ていなかった）
- **#717 / PR #733** **`glyphs.ts` に既定の枝を足す**——**知らない演算子が黙って無視されていた**
  （実データに 13 種類。`Tr 3` は不可視の文字、`gs` の `Font` は文字の x を 1 点に潰す）

**滋賀（147 本のうち 144 本で対応が確かめられた）**

- **#705 / PR #713** 行アイテム型 89 本。**20,068 対すべてで差が半セル未満**
- **#718 / PR #734** 1 文字 1 アイテム型 55 本。**10,908 対すべて**。**この型は行アイテム型より素直**

**調査の信頼性**

- **#710 / PR #719** **愛知に賛否の資料があった**（総数のみ → 会派別）。**「公開していない」の誤り 5 件目**
- **#721 / PR #735** **「無かった」の根拠を数え直した**——**A（本文だったか）は確かめた。B（全部だったか）は未確認**

**名寄せ**

- **#711 / PR #716** 一次資料どうしが氏名で食い違ったら `sourceConflict` として落とす
- **#722 / PR #736** **実際に起きる 4 機序では別人に決まらない**ことを固定（母数 1,123 / 別人 0）

**検査と記録**

- **#714 / PR #724** 変異が「落ちない」とき 3 つを分ける（当たっていない／等価／**検算が空回り**）
- **#727 / PR #737** **「落ちた」ときも狙った主張で落ちたとは限らない**（4 つ目の形）
- **#725 / PR #732** **PR の雛形が死んでいた**（#281 以降 274 本で不使用）
- **#720 / PR #738** **下限が実数から 16 離れていた**／`ci.yml` との二重定義がずれないことを見る
- **#709 / PR #715** `vitest` 3.2.7 → 4.1.11（GHSA-82fw-gwwq-j7x9）
- **#708 / #723** Sprint 27 の文書を進行中に書き始める／**`In Review` を飛ばしていた**

## この回でいちばん重い発見

**実装とテストが同じ未定義値を使うと、テストは恒真になる**（#699）。

`#693` で PO が `OPS.setTransform` を辿れと指示した。**pdfjs 6.2.108 に存在しない**（`undefined`）。
実装が `op === OPS.setTransform` と書き、テストも `OPS.setTransform` を投げていたら、
**`undefined === undefined` で通る。何も検査していないテストが緑になる。**

担当者が `tsc` の `TS2551` で気づいた。**型検査だけが捕まえた**——テストは捕まえない形だった。

**この回はもう 1 つ同じ形を出している**（#703）。PO が「`T*` の前に必ず `TD`/`TL` を出す」と誤って引用したが、
実測では **`T*` も `TD` も `TL` も 0 回**だった。担当者が 2 通りの数え方（pdfjs の `getOperatorList()` と
コンテンツストリームを `zlib` で解いた生トークン）で確かめて訂正した。

### 09-09 以降: **「無い」の検算は 4 通りに壊れる**

**この回は「変異テストが何も測っていない」形を 4 つ集めた。**
**3 つは担当者が自分で見つけ、1 つは別の担当者が同じ日に見つけた:**

| | 見た目 | 実際 | 見つけた人 |
|---|---|---|---|
| ① | 落ちない | **変異が当たっていない**（`mutate.sh` が exit 3） | #514（既出） |
| ② | 落ちない | **等価変異**（挙動が変わらない） | #712（`nextLine` が 2 枝に現れ、JS は先を取る） |
| ③ | 落ちない | **検算が空回り**（**測る対象そのものが減った**） | **#705**（`EDGE` 6.0 で対が 20,068 → 308） |
| ④ | **落ちる** | **狙った主張ではないものが落ちている** | **#710**（変異 5 が別の主張で落ちた） |

**③ がこの回で最も重い。** **「半セル以上 0 件」は保たれたまま、母数だけが消える。**
**#718 の担当者は最初から母数を判定に入れ、同じ変異（`EDGE` 6.0）を捕まえた**
（対が 10,908 → 672 に落ちるので落ちる）——**教訓が次の PBI で実際に効いた。**

**#724 の担当者は、#705 の主張を鵜呑みにせず実装の定数から独立に検算した**——
`EDGE`=6.0 では受け入れ幅が `セル幅 − 12.0` になり、**中央値 11.64pt では負**。
**PDF を読まずに「308 に減るのは当然」を示した。**

### **「無い」と書いた根拠そのものが揺れた**

**#710 が愛知で「HTTP 200・212 B の空 HTML（WAF）」を踏んだ。**
**過去の「無かった」はすべて 200 を根拠にしている。**

**#721 が数え直した結果**——**A（取ったページが本文だったか）の失敗は 1 件も無く、
表の値が動いた 5 件は 5 件とも B（そのページ集合で全部だったか）の失敗だった。**

**あわせて「大きさは測り方を書かないと比較できない」ことも分かった**——
**`Accept-Encoding` の有無で記録値が 3〜4 倍変わる**（#710 の「1,000 B 以上」は圧縮ありの値だった）。

## 落としたもの / PO が間違えたこと

**PO が Issue のクローズを 2 件落としていた**（#688 / #694）。

どちらも PR がマージ済み（`103fe614` / `b864bdec`）なのに Issue が `OPEN` のまま残り、
**PO が #688 に新しい担当者を立ててしまった。** 担当者が着手前に気づいて止めた
（WORKING_AGREEMENT の #512「同じ Issue のブランチが既にあれば先に訊く」）。

**Sprint 26 の「ボードを動かしていなかった」と同じ形である**——
ボードは動かすようになったが、**マージと Issue のクローズが繋がっていなかった。**

**PO が #695 で見立てを 4 か所すべて間違えた。**
三重は既に照合があり、徳島は月日だけ照合、宮城も年月照合が要り、
**島根は表題がある**（PO が `readPages` の上端 40pt を読んで説明文を拾った。
`parseVotePdf` の `title` は `第４９８回島根県議会（令和８年２月定例会）採決結果`）。

**教訓**: **既にあるパーサの出力を見る前に、生データを読んで判断しない**（#697 に記録）。

### 09-09 以降: **PO がボードの運用を 3 回続けて落とした**

| 回 | 落としたもの | 実測 |
|---|---|---|
| **Sprint 26** | **ボードを一度も動かさなかった** | Issue 260 件中 103 件しか載っていなかった |
| **#708** | **マージしたのに Issue を閉じていなかった** | #688 / #694。**PO はその状態で #688 に新しい担当者を立てた** |
| **#723** | **`In Review` を飛ばしていた** | PBI 5 件中 2 件しか通らず、その 2 件も PR から 20 分以上あと |

**道具（`board-set.sh`）はあり、書いてもある。実行の側が抜ける。**

**4 つ目も出た**——**マージ済みの worktree 5 本が残っていた**（#629 で 84 本まで溜まったのと同じ形）。
**PO が手で片付けて 7 本に戻し、#726 として起票した。**

### 09-09 以降: **PO が推した置き場所を担当者が根拠つきで退けた**（2 回）

**#714**: PO は `guards.md` を推したが、担当者は **「指させる成果物を持たない方法論を索引に入れると、
偽のパスを書くことになり索引が腐る」**として `WORKING_AGREEMENT.md` を選んだ。
**`guards-inventory.test.ts` の docblock（「索引が古くなると、索引が無かったときより悪くなる」）を引いている。**

**#725**: PO は「案 A（実態に合わせて書き直す）が筋」と書いたが、
**担当者は案 A を採った上で、PO が測っていなかったことまで測った**——
**雛形は #281 以降 274 本で不使用**（全 440 本を数えた）、**原因は `gh pr create --body` で雛形が読まれないこと**。

### 09-09 以降: **週次のレート上限で 7 人全員が途中で止まった**

**2026-09-09 の作業中に上限（リセット 09-13）に当たり、PR は 1 本も出ていない状態で全員が停止した。**
**作業は消えず worktree に残っていたので、PO が 1 件ずつ検証を最後までやり直して仕上げた**
（#717 は `data/` 再生成と変異 7 件、#722 は測定の完走と検査 4 件の新規作成、#727 は調査から執筆まで）。

**「動いたから通す」をしなかったのがこの回で唯一の正しい判断である。**

### 09-09 以降: **この文書を書きながら、PO が同じ罠に 2 回落ちた**

**1. `pnpm install` を忘れた worktree で測って「78 件失敗」を得た**（#588 とまったく同じ形）。

```
Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'node-html-parser'
→ pnpm install 後: tests 1287 / pass 1287 / fail 0
```

**#724 の担当者は「新しい worktree だったので `pnpm install` を先に済ませてから測っている——
`node_modules` が無い状態の失敗を変異の結果と読み違える事故（#588 の `fail 2`）を避けるため」
と PR に明記していた。** **PO はそれを読んだ上で同じことをした。**

**2. この文書に書いた数字 2 つが、測った場所と違うブランチの値だった**
（テスト数 1,284 は #720 の worktree の値、下限 98 は PR #738 の値で `main` はまだ 84）。
**検算して直した。**

**どちらも「書く前に測る」ではなく「測った数字を書き写すときに、どこで測ったかを見ていない」形である。**

## 次に持ち越すもの

| Issue | 内容 | なぜ持ち越したか |
|---|---|---|
| **#726** | マージ済み worktree の掃除が誰の仕事でもない | **Ready。** 「消す機能」を作るので、他の担当者が動いていない時に着手する |
| **（未起票）** | **滋賀の実装**——#705 / #718 で **147 本中 144 本の対応が確かめられた** | **#537 は 2026-09-13 に決まった（照会しない）ので着手できる。** 実装前に決めること: 記号 10 個以上で行を選ばない（議長が消える）／行アイテム型のやり方を 1 文字型に流用しない（192 対ずれる）／**空の列だけ 1 ページ目から補う** |
| **（未起票）** | **滋賀の画像 PDF 3 本** | 文字層が無いので #705 / #718 のどちらでも測れない |
| **（未起票）** | **B（そのページ集合で全部だったか）** | #721 は A だけを確かめた。**表の値が動いた 5 件は 5 件とも B の失敗** |
| **（未起票）** | 北海道・長野の会議録の留保 | #690 は「**開いた 5 本には無かった**」まで。記名投票を行った回を排除できていない |
| **（未起票）** | 愛知の会議録本文 | **#710 で「県議会サイト側に会議録は無い」を確定**（57 ページを実読）。**会議録システムは `robots.txt` が `Disallow: /` なので到達しない** |
| **（未起票）** | 佐賀の `猪村理恵子` / `猪村利恵子` | **どちらが正しいか決まらない。** 2 対 1 で `利` だが最新 PDF が `理`。**#711 の `sourceConflict` で落ちる側に倒してある** |
| **（未起票）** | **16 件が別人に決まる件** | **#722 で「実際に起きる 4 機序では 0 件」と測った。** 名簿が変わって 1 文字違いの現職が増えたら再発する（検査で固定済み） |
| ~~**#537**~~ | ~~地方議会への事前照会~~ | **2026-09-13 に決まった（案 3。照会しない）。未実装 9 県の ETL が解禁された** |
| **#610 / #654** | 404 ページの `site.conf` 反映（人間の ssh） | **本番で `/__not-found` が 200 のまま**（この回も実測） |
| **#543** | `git stash` 2 件の drop（人間の操作） | PO は消してよいと判断済み。権限で止められる |
| **#550 / #155 / #547** | fine-grained PAT の設置（人間の作業） | 守り自体は効いている |
| **#53 / #48 / #250** | Sponsors・広告・NDL 照会（人間の作業） | 急ぎではない |

## この回の終わり方

**#537（一次資料の取得と再公開）が人間の判断で決まり、未実装 9 県の ETL が解禁された。**

**Sprint 25 から 3 回にわたって「未実装 9 県の ETL 全部の前提」として持ち越されてきたものが消えた。**
**この回の作業 48 本は、すべて #537 を待たずに進められるものだった**——
**PDF 読みの穴・名寄せの安全性・調査の信頼性・検査の整備。**
**その積み上げが、解禁された瞬間に実装へ入れる状態を作っていた。**

**滋賀は #705 / #718 で 147 本中 144 本の対応が確かめられており、#741 で実装に入った。**

## 数字

| | |
|---|---|
| マージした PR | **48** |
| クローズした PBI | **22**（#670 #671 #674 #679 #680 #682 #688 #694 #703 #705 #707 #709 #710 #711 #714 #717 #718 #721 #722 #725 と、進行中の #720 #727） |
| `released` が `origin/main` から遅れている数 | **56 コミット**（回の終わりに実測） |
| 本番で 200 だったページ | **10 / 10**（`/__not-found` が 200 のままなのは #654） |
| 滋賀で対応が確かめられた PDF | **144 / 147**（#705 の 89 ＋ #718 の 55。残る 3 本は文字層が無い） |
| 47 都道府県の公開状況 | **公開 16 / 会派別 17 / 総数のみ 14 / 不明 0**（#710 で愛知が動いた） |
| 「公開していない」が誤りだった件数（3 日で） | **5**（熊本・山梨・滋賀・佐賀・**愛知**） |
| テストの数（`packages/etl`） | **1,287**（2026-09-13 実測。`pnpm install` を済ませた worktree で `node --test`） |
| テストファイルの下限と実数（ETL） | 実数 **101**（この回の頭は 89）。**下限は 84 のまま**で、#720（PR #738）が 98 に上げる |
