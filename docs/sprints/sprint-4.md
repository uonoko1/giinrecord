# Sprint 4（2026-08-22 〜 08-23）

## ゴール
議員の「やったこと」が揃い、検索から見つかる。

## レビュー（成果）
- ✅ 達成（一部は一次資料の制約で縮小）。
- 提出法案（#56）：参法の筆頭発議者を timeline に。**発見：参院の議案情報・提出法律案PDF・法制局一覧のいずれにも共同発議者名は載らない**（「外N名」のみ）。方針どおり「載っている名前だけ記録、N名は原文のまま表示」。一次資料探索を #63 として起票。
- SEO（#57）：sitemap.xml（601 URL）、robots、canonical、OGP。議員ページの title は「{氏名}（参議院・{選挙区}）の投票記録」。本番ビルドに SITE_ORIGIN を配線（#65）。
- データ一括配布（#49）：`/data/data-archive.zip`（決定的ビルド、601ファイル、5MB、CC BY 4.0 と出典 README 同梱）。
- 計測（#58）：nginx ログ集計方式（IP も UA も記録しない）。**レビューが本物のセキュリティ問題を阻止**：deploy 鍵ユーザーを adm グループに入れる実装は共用 VPS の他サイトのログを読めてしまうため差し戻し → root cron ＋集計結果のみ渡す方式に。VPS 側の sudo 作業は人間待ち。
- 会派履歴（#24）：採決時点の会派で名寄せ。実データで 2名（無所属→参政党 75票、無所属→立憲 136票）が group-mismatch.json に事実として記録され、票は失われない。
- 完了：#56 #57 #49 #58 #24（19pt）。4スプリント累計 80pt。

## レトロスペクティブ（検査と適応）

| 起きたこと | 原因 | 適応 |
|---|---|---|
| #56 の受け入れ基準が一次資料に無い項目（賛成者名）を前提にしていた | PO が衆院経過ページ（提出者・賛成者あり）の構造を参院にも当てはめた | 受け入れ基準を書く前に、該当ページを1枚開いて項目の存在を確認する（Sprint 3 の「いつ時点か」に「何が載るか」を追加） |
| #60 が 3 ファイルで衝突（package.json の build 連結、about.tsx、smoke.ts） | 4並列のうち3本が「ビルド後処理」と「about への節追加」を同時にした | 「ビルドパイプラインに段を足す PBI」は同一 wave に1本まで。about.tsx は節ごとにコンポーネント分割して衝突面を減らす（技術課題として起票） |
| #58 のセキュリティ差し戻し | 共用 VPS という前提が Issue に書かれていなかった | インフラ PBI の受け入れ基準に「共用ホスト：他サイトの資産に触れない」を定型で入れる |
| `gh pr list --head` にブランチ名ではなく main を渡すバグ（PO のスクリプト） | 手元スクリプトの変数ミス | PO の定型操作（マージ待ち・ボード更新）をリポジトリの `scripts/po/` に置いてテストする（技術課題） |
| レビュー差し戻し 2/5、うち1件はセキュリティ | — | 継続。レビューは「共用環境」「権限」の観点を明示的にチェックリストへ |

## 計測
- ベロシティ：19pt
- PO の手作業：衝突解消の委譲1回、ワークフロー配線1回（SITE_ORIGIN）、本番 ETL 確認2回
