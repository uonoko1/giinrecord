# Sprint 5（2026-08-23）

## ゴール
衆議院に踏み出す——個人投票が無い院でも「事実」と「推定」を型と画面で分けて見せる。

## レビュー（成果）
- ✅ 達成。衆院議員465名（名簿 2026-02-18 時点）、衆院議案451件（提出者・賛成者＝事実、会派態度＝推定）。衆院議員ページは「衆議院は個人の投票記録が公開されていません。」の注記、件数帯は提出／賛同／発言、会派態度は破線の判＋「会派の態度（推定）」ラベル。参院ページは byte-identical。
- /members は両院（院セレクト）、Home に衆院議員数。/about は節コンポーネント化（HTML スナップショットで同一性を担保）。PO 定型操作を scripts/po に。
- 完了：#71 #72 #73 #74 #64 #69 #70（23pt）。5スプリント累計 103pt。
- 発見：衆院サイトの議員プロフィール URL は選挙ごとに振り直される連番で安定IDにならない（Wayback で検証）→ `h_` + sha1(氏名＋かな)。
- 決定：サービス名「議会ログ」/ gikailog.jp、アプリと nginx は Docker 管理（#84 #85 #86 を Sprint 6 へ）。

## レトロスペクティブ（検査と適応）

| 起きたこと | 原因 | 適応 |
|---|---|---|
| PO が手で編集した etl.yml のインデントが崩れ、GitHub がワークフローを解析できなくなった（dispatch 不可） | PO の手編集＋ actionlint を CI で全ワークフローに掛けていなかった | 済：CI に actionlint（全ワークフロー）。**PO もワークフローを触るなら PR に actionlint を通してから**（手元で `pnpm dlx` できないため CI 任せでよい） |
| データ PR が BEHIND になって auto-merge が止まり ETL が失敗（自動 Issue #79 は正常に作られた） | strict status checks と並行マージ | 済：待機ループで update-branch。自動 Issue → 修正 → クローズの流れが初めて本番で回った |
| scripts/po/merge-when-green.sh が BEHIND を扱えず停止 | 同上を PO スクリプト側でも見落とし | #89 で修正（fake gh テスト付き） |
| 衆院ページが #73 前に公開されると誤解を招く表示になる懸念 | #71 と #73 の依存 | Wave 2 を即時起動し、日次 ETL 前にマージ。「公開される見え方を変える PBI は、データを増やす PBI と同じ wave か先」を計画ルールに |
| レビュー差し戻し 3/7（#73 は2往復） | 事実/推定の境界が細かい（回次ずれ、validator の網羅） | 継続。境界が細かい PBI は受け入れ基準に不変条件を列挙する |

## 計測
- ベロシティ：23pt（最大）
- PO の手作業：ワークフロー修正2回（YAML 崩れ、BEHIND）、スクリプト不備の補完1回
