# Sprint 12（2026-08-24）

## ゴール
1998年への遡り基盤、地方議会を7議会へ、収録範囲の明示。

## レビュー（成果）
- ✅ 全 PBI 完了（16pt）。累計 250pt。
- **#217 spike**: 参院の投票結果は第142回（1998-01-14）から存在し、**HTML 構造は現行の旧レイアウトパーサと一致**。議員名簿は第216回以降のみ（同一 UA・同一セッションの対照実験で不在を確認）。
- **#219 遡り対応**: 第142〜199回のパースに対応。`unmatched.json` を行の id から回次を導いてシャード（546KB→28KB、誤配0、後方互換）。404 のみを「ページ無し」とし 5xx/timeout は失敗させる修正。分割 dispatch（10〜15回次/回）を `docs/ops/etl.md` に記載。**バックフィル実行は Sprint 13**。
- **#220 高知（pref-39）** 104 表決 / **#221 島根（pref-32）** 112 表決 を追加し **7議会**に。両県とも不明セル0・未突合0。
- **#218 収録範囲ページ `/coverage/`**: 全数値をデータから算出。国会は院ごとに「個人別の投票」と「議案情報」を分離（衆院の議案1,941件・第195—221回が可視化された）。歯抜け回次（参院は22回次中11回次のみ記名投票）も表示。
- **#216 gitleaks**: 誤検知を発生源（フィクスチャのトークン）でサニタイズ。検知力の低下ゼロ。
- 本番規模: 9議会（国会2＋地方7）、議員 1,057名、国会採決 380件、地方表決 1,089件（宮城160・三重365・奈良125・鳥取118・島根112・徳島105・高知104）。

## レトロスペクティブ（検査と適応）

| 起きたこと | 原因 | 適応 |
|---|---|---|
| **PO が誤った前提で受け入れ条件を書き、レビューが訂正**（「cli は unmatched で非0終了する」→実際は warn のみ。契約が既に規定済み） | PO がコードを読まずに実装者の報告を信じた | **PO は受け入れ条件に「既存の挙動」を書く前に実コードを確認する**。契約に既にあることを二重に書かない |
| **検算が順序不変で列ずれを検出できなかった**（高知: 全行1列シフトで24行中0行しか落ちない） | 件数の照合は並べ替えに不変。しかも PR/契約に「列ずれを検出する」と実態より強く書いていた | 済：`WORKING_AGREEMENT.md` に明文化。名指しアサーション必須＋実際にシフトさせて確認＋効能を誇張しない |
| **島根が本番 ETL で落ちた**（6月は成功、2月定例会の PDF が別構造） | フィクスチャが1会期分のみ。レビューでもこの境界を見落とした | 済：`WORKING_AGREEMENT.md` に「作りの違う2つ以上の実データでテストする」を追記 |
| **在職未確認の氏名一致で18,401票が `m_` に紐づいていた**（実装中に発覚） | `resolveMember` が回次カバレッジを見ない。名簿に `termStart` が無い | #230 に切り出し（影響を測ってから一律解消）。この PR では**推定を含む紐づけであることを3箇所に明記**して隠さず公開 |
| 国会 0.5秒 / 地方 1秒 の**二重基準**が発覚（レビューが PO の指示の誤りを指摘） | 経緯で別々に実装された | #231 に起票（58回次の遡りで負荷増。過剰反応で更新頻度を落とさないバランスで決める） |
| Fable 5 のレート上限で実装5人が全滅 | — | 未 push で損失ゼロ。モデル切替後に再起動して回復 |
| canary（合成シークレット）9ファイルが作業ツリーに残存 | セキュリティ検証の後片付け漏れ | PO が発見・削除（履歴には未混入）。検証用ファイルは scratchpad に置く |

## 良かったこと（継続）
- **レビューが実際に手を動かした**: gitleaks CLI を入れて OR 結合を分解検証、グリフ x 座標を実測、変異テスト、実データのラウンドトリップ、一次資料の再取得。「読んで妥当そう」で通した PR がゼロ
- **実装者が受け入れ条件の未達を隠さず PO に上げた**（#219）。握りつぶさない文化
- レビュアーが**自分の誤りを自ら訂正**した（島根の検算に関する初回の記述）
- 差し戻し 4/6（高知・gitleaks・収録範囲・spike）。すべて実害のある指摘だった

## 計測
- ベロシティ: 16pt（累計 250pt）
- 差し戻し率 67%（前スプリント 0%）。**レビューの厳格化が効いた結果**であり、品質低下ではない
