# Sprint 10（2026-08-24）

## ゴール
地方議会を2つ増やし（徳島・鳥取）、国会を2019年まで遡る。

## レビュー（成果）
- ✅ 達成。徳島県議会（36名、2会期105採決、不明セル0）、鳥取県議会（35名、118採決。姓のみPDF は一意のときだけ突合、曖昧は候補列挙で未突合に）、国会 第200〜216回対応（旧テーブルレイアウト・起立採決・名簿404 の吸収、timeline の回次折りたたみ）。PDF 表復元を pdf-table.ts に共通化。shellcheck 一本化（#154）、verify-site（#182）。
- 本番：/assemblies/pref-36・pref-31 配信開始。第200〜216回のバックフィルは実行中（完了後データPR）。
- 例外対応（本番障害）：CSP が inline script を遮断しハイドレーション不全 → 'unsafe-inline' 許可＋CI に Playwright ブラウザ検証（#194）。実ブラウザで 768→1 行の絞り込みを確認。
- 完了：#183 #184 #103 #154 #182 #194（+3pt）＝ 23pt。累計 215pt。
- 決定：改名「議員レコード / giinrecord.jp」（#192、Sprint 11 先頭）。運用ルール「割り込み禁止・スプリントベース」を明文化。

## レトロスペクティブ（検査と適応）

| 起きたこと | 原因 | 適応 |
|---|---|---|
| 本番でクライアント機能が全滅していた（CSP）のに気づくのが遅れた | smoke は HTTP/HTML 検査のみで、ブラウザ実行を見ていなかった | 済：CI に Playwright（console error・CSP violation・絞り込み動作）。「ヘッダ・CSP の変更はブラウザ検証必須」を WORKING_AGREEMENT に |
| ワークフローを含むブランチの update-branch が OAuth の workflow スコープ不足で失敗 | gh トークンの制約（4度目の GITHUB_TOKEN/OAuth 制約） | merge-when-green.sh を「update-branch 失敗時は git merge + SSH push」にフォールバック（#起票） |
| 5本同時マージで CI 再実行の玉突きが発生し、マージに2時間 | 必須チェック strict × 並列 PR | マージは merge queue の代替として「1本ずつ、update→wait→merge」を PO スクリプトに統一。将来は GitHub merge queue の導入を検討 |
| バックフィル ETL が地方議会 ETL と同じ concurrency group でキャンセル | グループ名を共有していた | ワークフローごとに group を分ける（#起票） |
| 差し戻し 1/6（徳島の1往復のみ）。契約の事前共有（#156→ #157/#158）が効いた | — | 継続 |

## 計測
- ベロシティ：23pt（＋障害対応）
- 本番：議会5（国会2＋宮城・徳島・鳥取）、地方議員127名、地方採決383件
