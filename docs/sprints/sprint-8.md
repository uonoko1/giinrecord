# Sprint 8（2026-08-23）

## ゴール
運用が自走する（自前監視、冪等なデプロイ、リリース済み ref でのデータ配信）＋ 選挙区入口の仕上げ ＋ 地方議会拡張の調査。

## レビュー（成果）
- ✅ 達成。deploy スクリプトは引数検証・ポート空き検査・certbot 保護・`--force-recreate` で再実行が no-op（本番で確認、conf の md5 不変）。自前監視（Actions の外部プローブ 10分毎＋VPS の root cron）が初回実行で **本物の欠落（/data/meta.json 未配信）** を検出し Issue を自動作成 → #152 で修正中。Release は `released` タグを動かし、日次データ配信はそのタグのコードで行う（main の未リリースコードが本番に出ない）。選挙区入口の仕上げ（select 初期値、市区町村名）。地方議会 67 議会の調査（個人別表決を公開：都道府県で 9、Phase 1 候補：宮城・徳島・鳥取・奈良・三重）。
- 完了：#141 #135 #134 #120 #128（18pt）。8 スプリント累計 159pt。production リリース済（released = 67b5466）。

## レトロスペクティブ（検査と適応）

| 起きたこと | 原因 | 適応 |
|---|---|---|
| 監視の初回実行が 3 件の Issue を作った（うち実害は meta.json 未配信） | 監視側の前提（/data/meta.json）をプロダクト側が満たしていなかった | 監視 PBI の受け入れ基準に「プローブ対象 URL が実在することを smoke で担保」を入れる。今回は #152 で解消 |
| CI の shellcheck が新ディレクトリ（deploy/monitor）を対象にし、実装者のローカル実行と差が出た | shellcheck の対象 glob が CI と手元で違う | `scripts/ci/shellcheck.sh` に一本化し、手元と CI で同じコマンドにする（#153 起票） |
| 5 並列でも差し戻し 0/5、衝突 0 | ファイル所有の明示と、横断 PBI を混ぜなかったこと | 継続 |
| VPS の監視トークン（fine-grained PAT）は API で作れない | GitHub の仕様 | VPS→GitHub の Issue 作成は PAT が要る。人間作業として #53 と同じ「任意」枠に。Actions 側監視だけでも成立する |

## 計測
- ベロシティ：18pt
- PO の手作業：VPS 適用とリリース 10 分、shellcheck 修正 1 件
