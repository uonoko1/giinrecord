# Sprint 25（2026-09-08〜09）

## ゴール

**「守りがある」と「守りがあると思っている」を分ける。**
Sprint 24 が「守っていると書いてあることが事実か」を確かめる回だったのに対し、
この回は **PO 自身が「守りが無い」と誤読した実例が 1 日で 6 回出た**ので、
**そこから仕組みを作る**回になった。

**PO は実装せず、Ready の PBI を開発者に渡して回した。**

## 本番に出したもの（「リリースされて初めて成果物」）

| PBI | 成果 | 本番実測 |
|---|---|---|
| **#646** | 島根の一次資料 URL 3 件が 404 だったのを再生成で直す | `/assemblies/pref-32` の URL **11 件すべて 200**（前: `saikin` 2 件が 404） |
| **#648** | 議案名の `⾧`（康熙部首 U+2FA7）を `長` にする | `議⾧辞職の件` → **`議長辞職の件`**。判と凡例の `議⾧` は原文のまま残る |
| **Release** | `released` タグが 69 コミット遅れていたのを解消 | `567c09df`（09-06）→ **`044f42af`**。主要 9 ページすべて 200 でデグレ無し |

**#611（404 ページ）は Release でコード側だけが出た。** `site.conf` の反映（ssh）が残っており、
その中間状態を **#654** として起票してある。

## マージしたもの（26 PR）

**氏名と文字コード**
- **#632** かなとの比で氏名の欠落を検算（閾値 3.5。実データ最大 3.00）
- **#636** 7 県の氏名突き合わせを `name-match.ts` に統一。**国会とは分けたまま固定**
  （国会の `濵邊邉` を地方に入れると「渡邊」と「渡辺」が別人でも同じキーになる）
- **#648** 議案名だけ正規化。`vote.raw`・凡例・氏名は原文のまま

**検査の穴**
- **#640** `joinVertical` の並べ替えが 7 県で no-op であることを実測（1,650 回中 0）。
  **「テストが弱い」ではなく「その行が現在効いていない」**
- **#642** #505 で採らなかった枝のプローブ安全性の見本 5 件を取り込む
- **#652** シンボリックリンク門と `assert_confined` を潰しても落ちなかったのを塞ぐ
- **#661** Environment の `protection_rules` を見張る（設定は diff にも CI にも出ない）
- **#662／#665／#667** **守りの索引**（`docs/ops/guards.md`）を作り、育て、照合を強くした

**運用**
- **#629** worktree **84 本 → 1 本**。squash マージ済みの判定法を確定
- **#646** 一次資料リンクの死活を週 1 回確かめる `link-check.yml`
- **#659** `deploy.md` の「production は承認待ちになる」が実態と違っていたのを直す

## この回の中心：PO が 1 日で 6 回踏んだ誤り

**「探した場所に無い」を「存在しない」と読んだ。**

| 探して「無い」と思ったもの | 実際にあった場所 | 層 |
|---|---|---|
| 一次資料のリンク切れ検出 | `fetch.ts` の `res.ok` ＋ `etl.yml` | コード＋CI |
| 衆院の「推定」ラベルの検査 | `member-tabs.test.tsx` | テスト |
| 本番の新しい発言 | `/data/members/<id>/speeches.json` | データ |
| BMP 外文字の欠落の守り | `matchName` の部分列一致 | コード |
| 氏名正規化の規則の固定 | `name-normalization-table.test.ts` | テスト |
| 手動マージ後の `deploy-data` 起動 | `docs/ops/etl.md:66` | docs |

**6 件のうち docs にあったのは 1 件だけ。残り 5 件はコードとテストの中。**
**2 回は起票まで行った**（#634 は取り下げ、#636 は前提を訂正して残した）。

**これが #662（守りの索引）につながった。**

## 書いた原則

- **見つけたものを、そのまま「直すべきもの」と読まない**（1 日で 4 回踏みかけた）。
  **一覧を出したら、1 件ずつ「これは何か」を見る。数えて終わりにしない**
- **`grep | head` の結果を「無い」の根拠にしない**（1,466 行の 555 行目にあった）
- **手探りを始める前に `docs/ops/` を grep する**（6 例目は探してすらいなかった）
- **「取り消せないから」は人間待ちの理由にならない**（#629 で間違えた）

## 落としたもの

**ボードを一度も動かさなかった。**
ユーザーから「カンバンみても今まで何をやってたかと今何をやってるかが分からん」と指摘された。

```
Issue 260 件のうち Project に載っていたのは 103 件（157 件が未登録）
非 Done の 15 件のうち 13 件は既に CLOSED のまま放置
ボードの最新は #260、この回の作業（#632〜#668）は 1 件も載っていない
```

**`scripts/po/board-set.sh` は Sprint 4 から存在していた。使っていなかっただけ。**
**#669 で運用を `docs/ops/board.md` に書き、ボードを実態に復旧した。**

**教訓**: **成果物が本番に出ることと、進み方が外から見えることは別。**
**後者を落とすと、動いていても「動いていない」ように見える。**

## 次に持ち越すもの

**Sprint 23 にはこの節があったが、Sprint 24 と 25 の初版で落とした。**
**「次に何をやるか」が文書に無いと、次のスプリントの計画が毎回ゼロから始まる。**

| Issue | 内容 | なぜ持ち越したか |
|---|---|---|
| **#537** | 地方議会への事前照会（人間の判断） | **未実装 9 県の ETL 全部の前提。** これが決まらないと動けない |
| **#610 / #654** | 404 ページの `site.conf` 反映（人間の ssh） | Release でコード側だけ出た。中間状態が #654 |
| **#543** | `git stash` 2 件の drop（人間の操作） | PO は消してよいと判断済み。`git stash drop` が権限で止められる |
| **#550 / #155 / #547** | fine-grained PAT の設置（人間の作業）と、その症状の監視 Issue | branch protection の監視が読めない。**守り自体は効いている**（#547 のコメントで実測）。**#547 は PAT が付けば自動で閉じる** |
| **#53 / #48 / #250** | Sponsors・広告・NDL 照会（人間の作業） | 急ぎではない |

**Sprint 26 で作った PBI**（この回の調査から出たもの）:

| Issue | 内容 |
|---|---|
| **#670 / #671** | 47 都道府県の公開状況を全部確定（**不明 0 に。実装対象が 5 県 → 9 県**） |
| **#674** | 記号の字形の揺れを 7 県で 1 か所にまとめる |
| **#679** | ETL の失敗メッセージから県名とセル値が消えても落ちない |
| **#680** | 氏名が `□` に化ける問題をどう扱うか決める |

## 数字

| | |
|---|---|
| マージした PR | **26** |
| クローズした Issue | **15** |
| Release | **1**（69 コミット分） |
| 本番へのデータ配信 | **2** |
| worktree | 84 → **1**（main のみ。作業中の枝は別） |
| 守りの索引 | 0 → **55 行 / 103 逐語**（`MIN_ROWS` / `MIN_ANCHORS` の値。行はデータ行のみ、逐語はパスを除く） |
