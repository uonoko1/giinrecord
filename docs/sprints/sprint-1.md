# Sprint 1（2026-08-22）

## ゴール
第221回国会の参議院採決が、実データで、議員ページとして静的生成される。

## レビュー（成果）
- ✅ 達成。本番（VPS, nginx）で `/members/{id}` 247ページが事前生成され、各議員の記名採決120件が時系列・出典リンク付きで表示される。
- 完了 PBI：#1 #2 #3 #4 #5 #6 #9（28pt）。追加で #15 の大半（デプロイ基盤）。
- データ品質：120採決すべてで Σ会派人数 = 個人票数、名寄せ未突合 0、不変条件違反 0。
- 生まれた PBI：#24（会派履歴で名寄せ厳密化）、#26（可決/否決を議案情報から）。

## レトロスペクティブ（検査と適応）

| 起きたこと | 原因 | 適応（次スプリントから） |
|---|---|---|
| 5本中3本の PR にビルド成果物が混入し、レビューで差し戻し | `build/` を gitignore していなかった（PO の準備不足） | 済：gitignore ＋ hygiene テスト。スプリント0で「初回ビルド→git status が空」を DoD に |
| CI の「サーバーコード検出」が正当なビルド時処理（`process.env`）を弾いた | ガードがソース文字列ベースで粗かった | 済：配布バンドルに Node API が無いことを検査する方式に |
| `vitest.setup.ts` を3本の PR が同時に変更して衝突 | 共有ファイルの所有ルールに入っていなかった | WORKING_AGREEMENT の共有ファイル一覧に `vitest.setup.ts`, `.gitignore` を追加。テスト基盤はスプリント0で整える |
| ETL のデータ配信で3回失敗（保護ブランチ、PR作成権限、bot マージはイベントを発火しない） | GITHUB_TOKEN の制約を知らずに設計 | 済：PR＋auto-merge＋定期デプロイ。将来は GitHub App トークンを検討（#11 に記載） |
| Issue #2 の受け入れ基準「248名」が実際は247（欠員1） | PO が定数と在籍数を混同 | 受け入れ基準の数値は「一次資料のこのページのこの値」と書く |
| レビュアーが `--approve` できない（同一アカウント） | GitHub の仕様 | コメントレビューで verdict を残す運用で可。外部レビュアー参加時は本物の approve に |
| 全 PR が初回レビューで差し戻し（4/5） | 敵対的レビューが機能している証拠 | 継続。差し戻し理由の分布を追う（今回：成果物混入3、a11y 1、黙って空を返す 2、表記 1） |

## 計測
- ベロシティ：28pt / 1日（エージェント5並列）
- レビュー往復：平均 1.0 回
- PO の手作業：マージ順制御、衝突解消（.gitignore×2、vitest.setup.ts×2）、ETL/デプロイ配線
